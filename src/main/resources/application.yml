spring.application.name:
  TaskManager

spring.datasource:
  url:
    jdbc:h2:mem:testdb
  driverClassName:
    org.h2.Driver

spring.jpa:
  hibernate.ddl-auto:
    update
  defer-datasource-initialization:
    true

spring.h2.console.enabled:
  true

sping.jpa.show-sql:
  true